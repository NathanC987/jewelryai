# Shared Schemas

Shared contracts between frontend, backend, and downstream services.

## Current schema
- ring-parameters.schema.json

This schema is the early contract for real-time editable ring parameters in MVP.
