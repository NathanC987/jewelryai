# Frontend

Next.js shell for JewelryAI MVP UI.

## Run
- npm install
- npm run dev

## Notes
- Prompt-first generation is the default workflow in the right workbench panel.
- Sketch upload remains available in the left panel as an experimental seed mode.
- Three.js viewer loads backend-generated GLB artifacts and refreshes after create/update/export actions.
